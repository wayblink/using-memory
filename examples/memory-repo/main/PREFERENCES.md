# Preferences

- [pref] Prefer concise, direct replies with execution before explanation.
- [pref] Memory load order: read `<namespace>/PREFERENCES.md`, then `<namespace>/MEMORY.md`, then browse `<namespace>/docs/index.json` and load `<namespace>/docs/` documents on demand.
- [pref] Verify uncertain information before writing it to long-term memory.
